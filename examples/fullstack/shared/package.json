{
  "name": "@example/shared",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "zodem generate",
    "generate:check": "zodem generate --check",
    "buf:generate": "buf generate",
    "buf:lint": "buf lint proto",
    "buf:breaking": "buf breaking proto --against '../../../.git#ref=origin/main,subdir=examples/fullstack/shared/proto'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@bufbuild/protobuf": "2.15.0",
    "@zodem/codec": "workspace:*",
    "@zodem/core": "workspace:*",
    "zod": "4.6.5"
  },
  "devDependencies": {
    "@bufbuild/buf": "1.73.0",
    "@bufbuild/protoc-gen-es": "2.15.0",
    "@zodem/cli": "workspace:*",
    "typescript": "5.9.3",
    "vitest": "5.0.1"
  }
}
